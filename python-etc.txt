1. 내가 생성한 파일과 모듈이름이 겹치면 프로그램이 충돌하여 에러가 발생합니다.
=> random.py라는 파일을 생성하면 random 모듈과 충돌하게 됩니다.


2. python은 싱글 쓰레드로 동작하지만 멀티 쓰레드를 지원합니다.
=> GIL이라는 매커니즘이 존재해 멀티 쓰레드 동시 실행에 제약이 존재합니다.


3. python 인터프리터
=> python 실행 엔진을 의미합니다.
=> py는 인터프리터 언어라고도 하며, 코드가 실행될 떄 실시간으로 한 줄씩 해석하여 실행합니다. 이를 담당하는게 인터프리터입니다.


4. CPU 바운드 작업
=> CPU 계산 능력을 많이 사용하는 작업을 의미합니다.
=> 복잡한 수학 계산, 영상 처리, 데이터 처리 알고리즘 등을 의미합니다.

