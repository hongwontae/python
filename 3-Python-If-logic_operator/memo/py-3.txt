1. 파이썬의 if문은 if나 else키워드는 붙어있고 실행코드들은 띄어져있어야한다.
2. if(){} => x if 로직 : 이렇게만 적어야 한다.

if 로직 :
    if true일 떄 실행코드
else :
    if false일 떄 실행코드

3. if 중첩
if 로직 :
    실행코드
if 로직 :
    실행코드
=> if문으로만 구성되어 있다면 if문을 모두 통과해야한다.


4. elif
if 로직 :
    실행코드
elif 로직 :
    실행코드
=> if 로직이 실패하면 elif 로직을 살펴봐라



