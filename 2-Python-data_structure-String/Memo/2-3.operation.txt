1. /를 사용하면 float으로 변한다.
2. 대부분 일반적인 수학 법칙을 따른다.

3. round() => 반올림 함수
4. // 두 개를 사용해서 버림 연산할 수 있다.

5. +=, -=, /=, *= 연산이 가능하다.

6. F-string
=> f"{variable}"
=> f를 붙이면 JS의 템플릿 리터럴처럼 행동한다. {}를 사용한다.
=> f"Your Score is {point}" {}안에 변수를 사용하고 이는 문자열로 자동 변환된다.

7.여러 타입들이 스스로 형변환하여 연산을 하지 못한다.
=> 직접 형변환을 하여 연산을 한다. str()/bool()