1. __name__
=> Python에서 모든 모듈(파일)은 실행될 때 자동으로 __name__이라는 특별한 변수를 가집니다.
=> __name__은 모듈이 실행되는 방식에 따라 다르게 설정됩니다.

2. 스크립트를 직접 실행한 경우 (현재 실행 중인 파일의 __name__ == '__main__')
=> __name__의 값은 "__main__"이 됩니다.

3. 다른 모듈에서 import하는 경우

