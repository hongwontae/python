1. 데이터 타입 선언
=> py는 변수를 생성하고 데이터 타입을 선언할 수 있습니다.


2. 함수 return 타입 선언
def functionName (age : int) -> boolean :
    ...
=> return 값이 boolean이고 매개변수 age는 int 타입을 선언


** 타입을 지키지 않으면 컴파일 전에 알려줍니다.
** 이를 파이썬에서는 타입 힌트라고 합니다.